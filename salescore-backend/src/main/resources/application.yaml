spring:
  application:
    name: salescore

  datasource:
    url: jdbc:postgresql://localhost:5432/salescore_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080